**NOTE:**

1. before running this program run "npm install", this intalls all the modules required for react to run properly.
2. if any errors faced, run "npm audit fix --force".
3. after the errors are fixed then run "npm run dev", now the wesite is live (localhost).
